"""Шлюз MCP: строго ограниченный набор разрешённых операций для ИИ.

ИИ не получает прямого доступа к базе — только к перечисленным здесь
инструментам, каждый из которых применяет контроль доступа и пишет аудит.
Тот же реестр инструментов используется и standalone MCP-сервером
(``app.mcp.server``), и внутренним агентом Hermes (``services.hermes_agent``).
"""
